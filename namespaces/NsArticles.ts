export namespace NsArticles {
    export interface Article {
        id: string
        createdAt: string
        title: string
        preview: string
        image: string
        description: string
    }
}
